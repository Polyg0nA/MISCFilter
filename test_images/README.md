# Test Images Folder

Please place your 15 blurred test images in this folder (e.g. `image1.png`, `image2.jpg`, etc.).

The custom deblurring script will automatically detect all image files in this folder and restore them.
Supported formats: `.png`, `.jpg`, `.jpeg`, `.bmp`, `.tiff`, `.webp`
